trigger:
- master

variables:
- group: test
- name: namespace1
  value: 'yourNamespace1'
- name: capability1
  value: 'yourCapability1'
- name: namespace2
  value: 'yourNamespace2'
- name: capability2
  value: 'yourCapability2'

stages:
- stage: DisplayVariables
  displayName: 'Display Environment Variables'
  jobs:
  - job: Display
    displayName: 'Display'
    steps:
    - script: |
        echo "ENV_$(namespace1)-$(capability1): $ENV_$(namespace1)_$(capability1)"
        echo "ENV_$(namespace2)-$(capability2): $ENV_$(namespace2)_$(capability2)"
      displayName: 'Display environment variables'
